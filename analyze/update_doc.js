const fs = require('fs');

const content = `# ZCode TTFT 与 Token 速度显示说明（v2 精确版）

## 为什么之前 TTFT 只有 31ms / 速度偏大？（原因分析）
1. **TTFT 偏小的原因**：
   - 带有深度思考（Reasoning / Deep Think）的模型会先输出「思考过程（Reasoning）」，思考结束后才创建「回复正文（AssistantText）」行。
   - 之前的计时把创建回复正文的那一瞬间当作了起点，而正文首字在思考结束后的 31ms 就发出了，导致测出 31ms（完全忽略了用户点击发送到思考开始的网络延迟和数秒的思考耗时）。
2. **Token 速度与耗时偏小的原因**：
   - 之前只计算了最终正文文本的字数与传输耗时（0.3s），未将思考过程产生的几百个 Token 和思考耗时合并计算，导致算出了 250+ t/s 的瞬时传输速度。

---

## v2 精确版改进：
1. **真正端到端 TTFT 测量**：
   - 起点 $T_0$：用户提交 Prompt 的精确时间戳。
   - 首字点 $T_{\\text{first}}$：大模型产生第一块流式数据（无论是思考过程的第一字还是正文第一字）的网络到达时间戳。
   - $\\text{TTFT} = T_{\\text{first}} - T_0$（通常为 0.8s ~ 2.0s 真实网络与首字延迟）。
2. **多语言精准 Token 估算 + 思考/正文全量统计**：
   - 中文字符按 $0.72$ token/字符估算，英文按词与字符加权估算。
   - 完整统计「思考 Token 数 + 回复 Token 数」和整轮活跃生成耗时。
   - 呈现效果：\`⚡ TTFT: 1.25s · 48.3 t/s · 280 tok (思考 200 + 回复 80) · 5.8s\`
`;

fs.writeFileSync('temp/README.md', content, 'utf8');
